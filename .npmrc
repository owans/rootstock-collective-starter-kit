# GitHub Packages: required to install @rsksmart/collective-sdk (optional dependency).
# Set GITHUB_TOKEN with read:packages before npm install, or the app runs with the stub.
@rsksmart:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
