# When @rsksmart/collective-sdk is published to GitHub Packages (e.g. pre-release),
# set GITHUB_TOKEN with read:packages before npm install. Once the package is
# public on npm, this block is not required.
@rsksmart:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
